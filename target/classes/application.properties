# application.properties (src/main/resources)
# Konfiguracja SQLite ? aby po??czy? si? z baz? AWS, zmie? poni?sze ustawienia na dane po??czeniowe AWS RDS
spring.datasource.url=jdbc:sqlite:ai_task_manager.db
spring.datasource.driver-class-name=org.sqlite.JDBC

# Automatyczna aktualizacja schematu
spring.jpa.hibernate.ddl-auto=update

# Konfiguracja Spring Security (przyk?adowe dane ? zmodyfikuj w produkcji)
spring.security.user.name=admin
spring.security.user.password=admin
